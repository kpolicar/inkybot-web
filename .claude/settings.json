{
  "enabledPlugins": {
    "mcp-toolkit@docker": true
  },
  "mcpServers": {
    "docker": {
      "command": "docker",
      "args": ["mcp", "gateway", "run", "--transport", "stdio"],
      "env": {
        "DOCKER_MCP_IN_CONTAINER": "1"
      }
    }
  }
}
